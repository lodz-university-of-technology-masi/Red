plugins { id "io.spring.dependency-management" }

jar {
    baseName = 'red-repository'
}

dependencyManagement {
    imports { mavenBom("org.springframework.boot:spring-boot-dependencies:${springBootVersion}") }
}
dependencies {
    compile project(':red-model')
}
